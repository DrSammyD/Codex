import { Limit } from './../../models/limit';
import { Effect } from './../../models/effect';
//import { IsReady } from '../limits/isReady';
export const AttackDamage: Effect = {
  key: "AttackDamage",/*  keyof Effects; */
  channel: "attack"
}
